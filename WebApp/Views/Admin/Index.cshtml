@using NetBanking.Core.Application.ViewModels.Users;
@using NetBanking.Core.Application.Enums;
@model ICollection<UserViewModel>;

@{
    ViewData["Title"] = "Index";
}

<head>
    <script src="https://kit.fontawesome.com/7302c44b99.js" crossorigin="anonymous"></script>
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="stylesheet" href="~/css/tableadmin.css" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link href="https://fonts.googleapis.com/css2?family=Public+Sans:ital,wght@0,300;0,400;0,500;0,600;0,700;1,300;1,400;1,500;1,600;1,700&display=swap"
          rel="stylesheet" />
</head>

@* <div class="container-fluid">
    <div>

        <a class="btn btn-primary" asp-controller="Admin" asp-action="Register">New User <img src="~/Iconos/agregar-usuario.png" style="width: 30px; height: 30px;" /></a>
        <div class="card mt-4">
            <div class="card-body">
                <table class="table table-striped table-hover">
                    <thead>
                        <tr>
                            <th>Image</th>
                            <th>First Name</th>
                            <th>Last Name</th>
                            <th>Email</th>
                            <th>Identification</th>
                            <th>Is Active</th>
                            <th>Roles</th>
                            <th>Actions</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var user in Model)
                        {
                            <tr>
                                <td><img id="profileimage" src="@(!string.IsNullOrEmpty(user.ImageURL) ? user.ImageURL : "https://encrypted-tbn0.gstatic.com/images?q=tbn:ANd9GcQTnB8SJzsxM_KKbFNf-er2X-WiEdHMHXuz6w&usqp=CAU")" /></td>
                                <td>@user.FirstName</td>
                                <td>@user.LastName</td>
                                <td>@user.Email</td>
                                <td>@user.IdCard</td>
                                @if(user.IsActive == true)
                                {
                                    <td>Active</td>
                                }
                                else
                                {
                                    <td>Inactive</td>
                                }
                                <td>
                                    @foreach (var role in @user.Roles)
                                    {
                                       @role[0]
                                    }
                                </td>
                                <td>
                                    <button type="button" class="btn btn-primary" data-bs-toggle="modal"
                                            data-bs-target="#myModal">
                                        <i class="fa-solid fa-pen-nib"></i>
                                    </button>
                                    <a class="btn btn-warning" asp-route-Id="@user.Id" asp-controller="Admin" asp-action="Edit"><i class="fa-solid fa-pencil fa-sm" style="color: #19191a;"></i></a>
                                    <a class="btn btn-danger" asp-route-Id="@user.Id" asp-controller="Admin" asp-action="Remove"><i class="fa-regular fa-trash-can"></i></a>
                                    <a class="btn btn-info" asp-route-Id="@user.Id" asp-controller="Admin" asp-action="ChangeStatus">Activate User</a>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div> *@


<a class="btn btn-primary" asp-controller="Admin" asp-action="Register">New User <img src="~/Iconos/agregar-usuario.png" style="width: 30px; height: 30px; margin-left: 7px;" /></a>

<!-- Basic Bootstrap Table -->
<div class="card" style="margin-top: 50px;">
    <h5 class="card-header"><img src="~/Iconos/lista.png" style="width: 30px; height: 30px; margin-right: 7px;" /> List Register</h5>
    <div class="table-responsive text-nowrap">
        <table class="table">
            <thead>
                <tr>
                    <th>Role</th>
                    <th>Name and LastName</th>
                    <th>Users</th>
                    <th>Status</th>
                    <th>Email</th>
                    <th>Actions</th>
                </tr>
            </thead>
            @foreach (var user in Model)
            {

                <tbody class="table-border-bottom-0">
                <tr>
                    <td>
                        @foreach (var role in @user.Roles)
                        {
                            @if (role == RolesEnum.Client.ToString())
                            {
                                <i class="me-3"><img src="~/Iconos/letra-c.png" style="width: 25px; height: 25px;" /></i>
                                <span class="fw-medium">Cliente</span>
                            }
                            @if (role == RolesEnum.Admin.ToString())
                            {
                                <i class="me-3"><img src="~/Iconos/Angular_full_color_logo.svg.png" style="width: 25px; height: 25px;" /></i>
                                <span class="fw-medium">Administrador</span>
                            }
                            @role[0]
                        }
                    </td>
                    <td>@user.FirstName @user.LastName</td>
                    <td>
                        <ul class="list-unstyled users-list m-0 avatar-group d-flex align-items-center">
                            <li >
                                    <img src="@user.ImageURL" class="rounded-circle" style="width: 35px; height: 35px;" />
                            </li>
                        </ul>
                    </td>
                    @if(user.IsActive == true)
                    {
                            <td><span class="badge bg-label-success me-1">Active</span></td>
                    }
                    @if (user.IsActive == false)
                    {
                            <td><span class="badge bg-label-danger me-1">Inactive</span></td>
                    }
                    <td><span class="">@user.Email</span></td>
                    <td>
                        <div class="dropdown">
                            <button type="button" class="btn p-0 dropdown-toggle hide-arrow" data-bs-toggle="dropdown">
                                    <img src="~/Iconos/mas.png" style="width: 20px; height: 20px;" />
                            </button>
                            <div class="dropdown-menu">
                                @if (user.IsActive == false)
                                {
                                        <a class="btn btn-info" asp-route-Id="@user.Id" asp-controller="Admin" asp-action="ChangeStatus"><img src="~/Iconos/cambiar.png" style="width: 25px; height: 25px; margin-right: 7px;" /> Active</a>
                                }
                                @if (user.IsActive == true)
                                {
                                        <a class="btn btn-info" asp-route-Id="@user.Id"  asp-controller="Admin" asp-action="ChangeStatus"><img src="~/Iconos/apagar.png" style="width: 25px; height: 25px; margin-right: 7px;" /> Inactive</a>
                                }
                                    <a class="btn btn-warning" asp-route-Id="@user.Id" asp-controller="Admin" asp-action="Edit"> <img src="~/Iconos/editar (2).png" style="width: 25px; height: 25px; margin-right: 7px;" /> Edit</a>
                                    <a class="btn btn-danger" asp-route-Id="@user.Id" asp-controller="Admin" asp-action="ProductAdd"><img src="~/Iconos/expediente.png" style="width: 25px; height: 25px; margin-right: 7px;" /> Delete</a>
                            </div>                                                                                                                                                              
                        </div>
                    </td>
                </tr>
            </tbody>
            }
        </table>
    </div>
</div>
<!--/ Basic Bootstrap Table -->

<style>

    #profileimage {
        width: 100px;
        object-fit: cover;
        height: 100px;
        border-radius: 50%;
    }

    #card {
        border-radius: 6px;
    }

    .card {
        border-radius: 20px;
        box-shadow: 0px 4px 8px rgba(0, 0, 0, 0.1);
        transition: all 0.3s ease;
    }

    .carduser:hover {
        transform: translateY(-5px);
        box-shadow: 0px 6px 12px rgba(0, 0, 0, 0.2);
    }
</style>
